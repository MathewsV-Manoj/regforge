# RegForge reads your key from the environment.
ANTHROPIC_API_KEY=sk-ant-...

# Optional: where the verified corpus lives.
# Defaults to ./corpus when present, else ~/.regforge/corpus
# REGFORGE_CORPUS=./corpus
